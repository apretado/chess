package chess;

import java.util.ArrayList;

public class PieceMovesCalculator {
    public static ArrayList<ChessMove> bishop(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }

    public static ArrayList<ChessMove> king(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }

    public static ArrayList<ChessMove> knight(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }

    public static ArrayList<ChessMove> pawn(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }

    public static ArrayList<ChessMove> queen(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }

    public static ArrayList<ChessMove> rook(ChessBoard board, ChessPosition myPosition) {
        var moves = new ArrayList<ChessMove>();
        return moves;
    }
}
